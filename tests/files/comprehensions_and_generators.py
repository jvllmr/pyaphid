from datetime import datetime

value = [print(datetime.now()) for _ in datetime.now()]
value = (print(datetime.now()) for _ in datetime.now())
value = {print(datetime.now()) for _ in datetime.now()}
value = {datetime.now(): datetime.now() for _ in datetime.now()}
